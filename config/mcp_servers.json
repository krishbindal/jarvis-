{
  "mcpServers": {
    "eugener-shell": {
      "command": "go",
      "args": ["run", "github.com/eugener/jarvis-mcp@latest"],
      "enabled": false,
      "description": "Structured shell and file tree access. (Needs Go install)"
    },
    "mock-server": {
      "command": "python",
      "args": ["test_mcp_mock_server.py"],
      "enabled": true,
      "description": "Local mock server for integration testing."
    },
    "open-interpreter": {
      "command": "py",
      "args": ["-3.12", "-m", "interpreter", "--mcp"],
      "enabled": true,
      "description": "Open Interpreter for universal system and code execution (via Python 3.12)."
    }
  }
}
